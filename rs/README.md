# RS-485 Microservice

gRPC-микросервис для взаимодействия с драйвером RS-485.

> **Проект предназначен для сборки и запуска под Linux (Ubuntu 24.04 или совместимые дистрибутивы).**

## Содержание

* Структура проекта
* Требования
* Сборка
* Очистка
* Запуск
* Генерация gRPC-кода

---

# Структура проекта

```text
rs/
├── api/                 # .proto-файлы драйвера и микросервиса
├── client/              # gRPC-клиенты драйвера и микросервиса
├── common/              # Общие типы, обработка ошибок и вспомогательные функции
├── service/             # Реализация микросервиса
├── scripts/             # Скрипты сборки и очистки
├── build/               # Результаты сборки (создаётся автоматически)
├── CMakeLists.txt
└── README.md
```

---

# Требования

Для сборки проекта необходимы:

* Linux (Ubuntu 24.04 или совместимый дистрибутив);
* CMake 3.16 или новее;
* компилятор с поддержкой C++17;
* Protobuf;
* gRPC;
* pkg-config.

Установка зависимостей:

```bash
sudo apt update

sudo apt install \
    build-essential \
    cmake \
    pkg-config \
    protobuf-compiler \
    libprotobuf-dev \
    libgrpc++-dev \
    protobuf-compiler-grpc
```

Проверить установку:

```bash
cmake --version
protoc --version
pkg-config --modversion grpc++
```

---

# Сборка

Все команды выполняются из директории `rs`.

```bash
cd rs
./scripts/build.sh
```

Во время сборки автоматически:

* генерируется gRPC-код из `.proto`-файлов;
* создаётся директория `build`;
* выполняется конфигурация CMake;
* собирается исполняемый файл микросервиса.

---

# Очистка

Для удаления результатов сборки выполните:

```bash
./scripts/clean.sh
```

---

# Запуск

После успешной сборки запустите микросервис:

```bash
./build/rs485_microservice
```

Перед запуском микросервиса должен быть запущен драйвер RS-485.

Для остановки микросервиса нажмите:

```text
Ctrl + C
```

---

# Генерация gRPC-кода

Генерация gRPC-кода выполняется автоматически во время сборки.

Если были изменены `.proto`-файлы, достаточно повторно выполнить:

```bash
./scripts/build.sh
```

Сгенерированные файлы находятся в директории `build/gui_generated/` и не должны редактироваться вручную.

