syntax = "proto3";

import "google/protobuf/empty.proto";

package grpc_service_driver;

service ServiceDriver {
    rpc GetVersion (google.protobuf.Empty) returns (Version) {}
}

message Version {
    string version = 1;
}
