syntax = "proto3";

package pku.driver.v1;

service PkuDriver {
    rpc Add(AddRequest) returns (AddResponse);
}

message AddRequest {
    int32 number_1 = 1;
    int32 number_2 = 2;
}

message AddResponse {
    int32 sum = 1;
}
